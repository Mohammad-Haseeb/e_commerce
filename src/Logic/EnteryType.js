export  class EnteryType{

     constructor(userType){
           this.setUserType(userType);
     }

      setUserType(userType){
          this.userType=userType
     }
      getuserType(){
         return  this.userType;
     }
}

